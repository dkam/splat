<!-- Header Toolbar -->
<header class="fixed top-0 left-0 right-0 bg-white border-b border-gray-200 shadow-sm z-40">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo/Branding -->
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2 text-gray-900 hover:text-gray-700 transition-colors" do %>
          <div class="text-2xl font-bold text-blue-600">Splat!</div>
        <% end %>
      </div>

      <!-- Right side: System Status & Authentication -->
      <div class="flex items-center space-x-6">
        <!-- Queue Depth Indicator -->
        <div class="flex items-center space-x-2">
          <span class="text-sm text-gray-600">Queue Depth:</span>
          <% queue_depth_value = queue_depth %>
          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                <%= queue_depth_value == 0 ? 'bg-green-100 text-green-800' :
                    queue_depth_value < 10 ? 'bg-yellow-100 text-yellow-800' :
                    'bg-red-100 text-red-800' %>">
            <%= queue_depth_value %>
          </span>
        </div>

        <!-- Authentication Section -->
        <div class="flex items-center space-x-4">
          <% if authenticated? %>
            <!-- User Info -->
            <div class="flex items-center space-x-3">
              <div class="text-right">
                <div class="text-sm font-medium text-gray-900"><%= current_user_name %></div>
                <div class="text-xs text-gray-500">
                  <%= current_user_provider %>
                  <% if current_user_authenticated_at %>
                    • <%= time_ago_in_words(current_user_authenticated_at) %> ago
                  <% end %>
                </div>
              </div>

              <!-- Logout Button -->
              <%= button_to logout_path, method: :delete, class: "inline-flex items-center px-3 py-1.5 border border-transparent text-xs font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors", data: { turbo: false }, form: { class: "inline" } do %>
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                </svg>
                Logout
              <% end %>
            </div>
          <% else %>
            <!-- Login Options -->
            <div class="flex items-center space-x-2">
              <% if SplatAuthorization.oidc_configured? %>
                <!-- OIDC Login -->
                <%= link_to login_path,
                    class: "inline-flex items-center px-3 py-1.5 border border-gray-300 text-xs font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors",
                    title: "Login with OpenID Connect" do %>
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                  </svg>
                  Login
                <% end %>
              <% end %>

              </div>

            <!-- Configuration Status -->
            <% if !SplatAuthorization.oidc_configured? %>
              <div class="text-xs text-red-600 italic">
                OIDC not configured
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</header>