<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
</head>
<body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333;">
  <div style="max-width: 600px; margin: 0 auto; padding: 20px;">
    <div style="border-left: 4px solid #f59e0b; padding: 20px; background: #fffbeb; border-radius: 0 8px 8px 0;">
      <h1 style="margin: 0 0 10px 0; color: #b45309;">
        <%= @auto_ignored ? "🤖 Issue Auto-Ignored" : "🔥 Issue Burst Detected" %>
      </h1>
      <h2 style="margin: 0 0 20px 0; color: #78350f; font-size: 1.2em;"><%= @issue.title %></h2>
    </div>

    <div style="margin: 20px 0; padding: 15px; background: #f8fafc; border-radius: 8px;">
      <p style="margin: 0 0 10px 0;">
        This issue is emitting
        <strong><%= number_with_delimiter(@rate) %> events/hour</strong>
        (threshold: <%= number_with_delimiter(@threshold) %>/hour).
      </p>
      <p style="margin: 0 0 10px 0;"><strong>Exception Type:</strong> <%= @issue.exception_type || 'Unknown' %></p>
      <p style="margin: 0 0 10px 0;"><strong>Project:</strong> <%= @project.name %></p>
      <% if @auto_ignored %>
        <p style="margin: 0 0 10px 0;"><strong>Auto-ignored at:</strong> <%= @issue.auto_ignored_at.strftime('%Y-%m-%d %H:%M:%S UTC') %></p>
      <% end %>
    </div>

    <div style="margin: 20px 0;">
      <% if @auto_ignored %>
        <p style="margin: 0 0 15px 0;">The issue has been auto-ignored. Reopen it if this was a real bug worth tracking.</p>
      <% else %>
        <p style="margin: 0 0 15px 0;">
          This is just a heads-up — the issue is still tracked normally. You can enable
          auto-ignore in Settings → Noise Control if you want noisy issues silenced
          automatically.
        </p>
      <% end %>

      <a href="<%= @url %>"
         style="display: inline-block; background: #3b82f6; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 500;">
        View Issue
      </a>
    </div>

    <div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #e5e7eb; font-size: 0.9em; color: #6b7280;">
      <p style="margin: 0;">
        This notification was sent by <strong>Splat</strong> - your lightweight error tracker.
      </p>
    </div>
  </div>
</body>
</html>
